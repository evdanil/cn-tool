"""cn-lens object analysis package."""

# Component version for cn-lens. Bumped by hand when the tool's behaviour
# changes; the shared package build stamp is read separately from the repo-root
# ``version`` file via ``cn_buildstamp``.
__version__ = "0.6.0"
